{% comment %}
  Results toolbar: product count, clear all, sort and per-page (CLAUDE.md §10.3).
  Each element is individually toggleable by the merchant.
{% endcomment %}

{% render 'scfs-context' %}

<div class="scfs scfs-toolbar" data-scfs-toolbar {{ block.shopify_attributes }}>
  {%- if block.settings.show_count -%}
    <p class="scfs-toolbar__count" data-scfs-count aria-live="polite">
      {%- if collection.products_count -%}
        {{ 'toolbar.product_count' | t: count: collection.products_count }}
      {%- endif -%}
    </p>
  {%- endif -%}

  <div class="scfs-toolbar__controls">
    {%- if block.settings.show_per_page -%}
      <label class="scfs-select">
        <span class="scfs-select__label">{{ 'toolbar.show' | t }}</span>
        <select data-scfs-per-page>
          {%- assign options = '12,24,36,48' | split: ',' -%}
          {%- for option in options -%}
            <option value="{{ option }}">{{ option }}</option>
          {%- endfor -%}
        </select>
      </label>
    {%- endif -%}

    {%- if block.settings.show_sort -%}
      <label class="scfs-select">
        <span class="scfs-select__label">{{ 'toolbar.sort' | t }}</span>
        <select data-scfs-sort>
          {%- comment -%}
            Populated by the script from the app's sort list so admin settings
            and the storefront cannot drift. These are the no-JS defaults.
          {%- endcomment -%}
          <option value="manual">Featured</option>
          <option value="best-selling">Best selling</option>
          <option value="title-ascending">Alphabetically, A-Z</option>
          <option value="title-descending">Alphabetically, Z-A</option>
          <option value="price-ascending">Price, low to high</option>
          <option value="price-descending">Price, high to low</option>
          <option value="created-descending">Newest</option>
          <option value="created-ascending">Oldest</option>
        </select>
      </label>
    {%- endif -%}
  </div>
</div>

{% schema %}
{
  "name": "Filter toolbar",
  "target": "section",
  "stylesheet": "scfs.css",
  "javascript": "scfs.js",
  "settings": [
    {
      "type": "checkbox",
      "id": "show_count",
      "label": "Show product count",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_sort",
      "label": "Show sort control",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_per_page",
      "label": "Show products per page",
      "default": true
    }
  ]
}
{% endschema %}
